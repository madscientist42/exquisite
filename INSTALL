COMPILING and INSTALLING:

If you got a official release tar archive or a git repository pull do:
    cmake .
    
Then to compile:
    make

To install (run this as root, or the user who handles installs):
    make install

